version: "2"

linters:
  default: none
  enable:
    - loglint
  settings:
    custom:
      loglint:
        type: module
        description: "log message policy + sensitive logging detection"
        settings:
          enable_rule2_ascii_only: false
          extra_allowed_chars: ".:/"